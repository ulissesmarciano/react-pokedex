import styled from "styled-components";

export const Container = styled.div`
    height: 2.4rem;

    display: flex;
    align-items: center;
    svg {
        height: 1.2rem;
        margin: 0 .4rem -.3rem;
    }

    p {
        font-size: 1rem;
        font-weight: 600;
        color: #00000095;
    }
`